require 'spec_helper'

describe Nest::Monitor do
  it 'has a version number' do
    expect(Nest::Monitor::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
